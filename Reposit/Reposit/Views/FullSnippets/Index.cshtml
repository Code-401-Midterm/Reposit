@model IEnumerable<Reposit.Models.FullSnippet>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>


<div class="container-fluid">

    <div class="row">

    @foreach (var thing in Model)
    {
        <div class="col-md-3 preview-cards">
            <h4>
                @Html.DisplayFor(modelItem => thing.Title)
            </h4>
            <p>
                @Html.DisplayFor(modelItem => thing.Notes)
            </p>
                <span class="badge badge-primary" id="@Html.DisplayFor(modelItem => thing.Language)">@Html.DisplayFor(modelItem => thing.Language)</span>
            <p>
                <a asp-action="Details" asp-route-id="@thing.ID">View details »</a>
            </p>
        </div>
    }
    </div>
</div>



                @*<a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>*@
